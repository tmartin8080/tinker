version: '3'

services:
  app:
    build:
      context: .
    entrypoint: ./entrypoint.sh
    environment:
      SECRET_KEY_BASE: dl59l4FYtxcSfsWGCYEcE5Y5jOaSu9+6agSoLKc0aYI9up9WFfqwqKVlSGxF3b71
      MIX_ENV: dev
      DATABASE_URL: postgres://postgres:postgres@db:5432/tinker_dev
    ports:
      - "4000:4000"
    depends_on:
      - db

  db:
    image: postgres:9.6
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
    restart: always
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

